import { LenghtNumverPipe } from './lenght-numver.pipe';

describe('LenghtNumverPipe', () => {
  it('create an instance', () => {
    const pipe = new LenghtNumverPipe();
    expect(pipe).toBeTruthy();
  });
});
